@model IEnumerable<NursingChecklistManager.Models.UserChecklistModel>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Checklists)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Checklists.LineItemJoinerId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserChecklistId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserChecklistId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserChecklistId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
